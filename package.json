{
  "scripts": {
    "server": "exec-bin node_modules/.bin/hugo server --config=config.yml",
    "build": "exec-bin node_modules/.bin/hugo --gc --minify --config=config.yml"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "jspdf": "^2.5.2",
    "wrangler": "^3.82.0"
  },
  "otherDependencies": {
    "hugo": "0.88.1"
  },
  "devDependencies": {
    "exec-bin": "^1.0.0",
    "hugo-bin": "^0.132",
    "hugo-installer": "^4.0"
  }
}
